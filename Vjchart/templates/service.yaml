apiVersion: v1
kind: Service
metadata:
  name: {{ include "Vjchart.fullname" . }}
  labels:
    {{- include "Vjchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Vjchart.selectorLabels" . | nindent 4 }}
