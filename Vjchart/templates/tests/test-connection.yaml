apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Vjchart.fullname" . }}-test-connection"
  labels:
    {{- include "Vjchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Vjchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
